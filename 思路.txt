
使用aspectJ框架实现AOP：

使用AOP：目的是给已经存在的一些类和方法，增加额外的功能。前提是不改变原来的类的代码。

思路：
1、新建maven项目，加入spring，aspectJ，Junit等依赖，整理项目结构

2、创建示例接口以及其实现类（该实现类作为需要增加切面功能的目标对象）

3、创建一个普通类，使用相关注解，作为切面类，为目标对象增加功能。

4、在主配置文件中声明各对象和 自动代理生成器

5、创建测试类，从spring容器获取目标对象（此时已经是功能增前后的代理对象），调用方法进行测试